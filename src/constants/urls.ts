const baseURL = 'https://api.themoviedb.org/3/discover'

const movies = '/movie'
const urls = {
    movies: {
        movies,
       // byId: (id: number): string => `${cars}/${id}`,

    }
}

export {
    baseURL,
    urls
}
